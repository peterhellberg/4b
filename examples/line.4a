; line.4b - horizontal line across the middle
lda #8
sta r1
lda #0
sta r2
sta r3

@draw:
lda r2
sta r0
flip r0, r1
inc
sta r2
iflt r3         ; loop while new x > 0 (exit when it wraps to 0)
jmp @draw

@halt:
jmp @halt
