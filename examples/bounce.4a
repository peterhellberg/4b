; bounce.4a     ; line moving diagonally, wrapping at edges

lda #0
sta r1          ; x = 0
sta r2          ; y = 0

@loop:
  flip r1, r2   ; flip pixel (x, y)
  
  lda r1        ; x++
  inc
  sta r1
  
  lda r2        ; y++
  inc
  sta r2

  jmp @loop     ; repeat
