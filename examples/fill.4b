; fill.4b - fill the entire screen
lda #0
sta r1
sta r3

@row:
lda #0
sta r2

@col:
flip r2, r1
inc
sta r2
lda r2
iflt r3         ; x > 0? next column
jmp @col

lda r1
inc
sta r1          ; y++
iflt r3         ; y > 0? next row
jmp @row

@halt:
jmp @halt
